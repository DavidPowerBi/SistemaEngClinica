<h1>Editar OS</h1>
<form action="/os/edit/{{os.OS}}" method="post">
  <label for="SETOR">Setor:</label>
  <input type="text" name="SETOR" value="{{os.SETOR}}" required>
  <label for="PERIODO">Período:</label>
  <input type="date" name="PERIODO" value="{{os.PERIODO}}" required>
  <label for="EQUIPAMENTOS">Equipamentos:</label>
  <input type="text" name="EQUIPAMENTOS" value="{{os.EQUIPAMENTOS}}" required>
  <label for="ESTADO">Estado:</label>
  <input type="text" name="ESTADO" value="{{os.ESTADO}}" required>
  <label for="TIPO_EQUIP">Tipo de Equipamento:</label>
  <input type="text" name="TIPO_EQUIP" value="{{os.TIPO_EQUIP}}" required>
  <button type="submit">Salvar</button>
</form>
<a href="/os">Voltar</a>
